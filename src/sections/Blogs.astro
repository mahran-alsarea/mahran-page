---
import BlogPostPreview from "../components/BlogPostPreview.astro";
const allPosts = await Astro.glob("../pages/المدونة/*.md");
import { Image } from "astro:assets";
---

<header class="bleed">
  <div class="text-container">
    <h1>مدونة سبيد ستار لنقل الأثاث</h1>
    <p>
      تختار شركتنا لنقل الاثاث في جدة بالمملكة العربية السعوديه بسبب جودة
      خدماتنا وموثوقيتها. يتميز فريقنا المحترم بالخبرة والتدريب, مع التزامنا
      بالمواعيد والتسليم في الوقت المناسب والمدد . نستخدم تقنيات حديثة ومعدات
      متقدمة لضمان سلامة الاثاث خلال عمليات النقل. بأسعار تنافسية نقدم تجربة
      خدمة مريجة تلبي احتياجات وتوقعات عملائنا بكفاءة وامان.
    </p>
    <a href="#contactus" class="cta">إبدأ معنا الأن</a>
  </div>
</header>
<ul>
  {
    allPosts.map((post) => (
      //@ts-expect-error
      <BlogPostPreview url={post.url} title={post.frontmatter.title}>
        <Image src={import("../assets/hero1.jpeg")} alt="worker" />
      </BlogPostPreview>
    ))
  }
</ul>
<style>
  header {
    position: relative;
    height: 70dvh;
    background: lightblue;
  }
  .text-container {
    width: 100%;
    max-width: var(--container-width);
    padding: var(--container-inline);
    margin-inline: auto;
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    color: var(--text-secondary-light, #fff);
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 2rem;
  }
  .text-container h1 {
    font-style: normal;
    font-size: clamp(2rem, 4vw, 3.5rem);
    font-weight: 700;
    line-height: normal;
  }
  .text-container p {
    color: var(--text-secondary-light, #fff);
    text-align: center;
    font-family: Cairo;
    font-size: clamp(1rem, 2vw, 1.5rem);
    font-style: normal;
    font-weight: 400;
    line-height: normal;
    text-align: start;
  }
  .text-container a {
    color: white;
    text-decoration: none;
    border-radius: var(--borderRadius-sm, 8px);
    background: var(--bg-primary-accent-heavy, #f9710d);
    box-shadow: 0px 5px 30px 0px rgba(137, 153, 163, 0.24);
    display: inline-flex;
    height: 48px;
    padding: 2rem;
    justify-content: center;
    align-items: center;
    gap: 8px;
    flex-shrink: 0;
  }
  .cta:hover {
    transform: scale(1.15);
  }
  @media screen and (max-width: var(--sm)) {
    .text-container {
      white-space: normal;
    }
  }
  ul {
    display: grid;
    width: 100%;
  }
  img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
</style>
